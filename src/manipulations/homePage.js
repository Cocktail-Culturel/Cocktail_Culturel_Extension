import waitForElement from "../helpers/waitForElement";

export default function homePageManipulation(config) {
  /*
  waitForElement(config.view).then(() => {
    document.querySelectorAll(config.view).forEach((e) => {
      if (e.querySelectorAll(".cc-link").length == 0) {
        e.innerHTML += `<a href="#" class="cc-link">Cocktail Culturel</a>`;
      }
    });
  });
  */
}
